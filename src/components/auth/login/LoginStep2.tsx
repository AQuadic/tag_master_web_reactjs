import React from "react";

interface LoginStep2Props {
  setStep: React.Dispatch<React.SetStateAction<number>>;
}

const LoginStep2 = ({ setStep }: LoginStep2Props) => {
  return <div>LoginStep2</div>;
};

export default LoginStep2;
