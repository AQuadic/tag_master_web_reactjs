import MainCart from '@/components/cart/MainCart'
import React from 'react'

const page = () => {
    return (
        <section>
            <MainCart />
        </section>
    )
}

export default page
